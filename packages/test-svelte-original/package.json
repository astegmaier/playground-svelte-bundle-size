{
  "name": "test-svelte-original",
  "version": "1.0.0",
  "private": true,
  "description": "Bundles svelte/store with a stock install of svelte (no patch).",
  "scripts": {
    "build": "rm -rf dist && webpack --env raw && webpack"
  },
  "dependencies": {
    "svelte": "5.55.4"
  },
  "devDependencies": {
    "@swc/core": "^1.10.0",
    "terser-webpack-plugin": "^5.3.10",
    "webpack": "^5.97.1",
    "webpack-cli": "^5.1.4"
  }
}
